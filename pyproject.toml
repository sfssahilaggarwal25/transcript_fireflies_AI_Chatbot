[project]
name = "fireflies-transcript-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.1",
    "load-dotenv>=0.1.0",
    "requests",
    "uvicorn>=0.46.0",
]
